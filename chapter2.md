# ruby的方法调用

主要有两种方法：

1. 动态派发

2. 重载method_missing方法（这个比较有趣也比较常用）

## 关于第二种的一些细节

method_missing一般有两个参数 *(这是我目前明白的两个参数，实际上还有别的)*
第一个是一个符号类型的量，可以取名为name，表示你在主程序要调用的方法名称；通常，当需要对这个name做操作的时候，用to_s方法将其转换为字符串类型

第二个是参数 *args，这个是你的方法在调用时需要的参数，个数不限。这个量可以认为是个数组，依次存放方法需要的参数。

待补充...
